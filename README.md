# actions-github-ref

Define `GITHUB_BRANCH` and `GITHUB_TAG` environment variable based on `GITHUB_REF` for next workflows.

- GITHUB_REF: `refs/heads/master` -> GITHUB_BRANCH: `master`
- GITHUB_REF: `refs/tags/1.0.0` -> GITHUB_TAG: `1.0.0`

# Contributpr

- @keroxp


# LICENSE

MIT
